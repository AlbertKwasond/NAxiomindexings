@model AxiomAdmin.ViewModel.TeamContextViewModel
@foreach (var item in Model.ListTeamContexts)
{
    <div class="col">
        <div class="card h-100">
            <img class="card-img-top" src="@Url.Content("https://localhost:7043/assets/img/team/"+item.ImageUrl)" alt="Card image cap" height="466" />
            <div class="card-body">
                <h5 class="card-title">
                    @Html.DisplayFor(model => item.Name)
                </h5>
                <p class="card-text">
                    @Html.DisplayFor(model => item.Position)
                </p>
                <a href="#" data-id="@item.Id" class="card-link edit-item-btn">Edit</a>
                <a asp-action="Delete" asp-controller="YourControllerName" asp-route-id="@item.Id" class="card-link">Delete</a>
            </div>
        </div>
    </div>
}
